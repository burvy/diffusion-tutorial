[project]
name = "diffusion-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "einops>=0.8.2",
    "torch>=2.13.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
